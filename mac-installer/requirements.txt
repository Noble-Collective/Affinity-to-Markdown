# Shared deps for both Windows and macOS desktop apps.
# Install into a Python 3.11 venv:
#   python -m venv venv311
#   pip install -r requirements.txt

marker-pdf==1.10.2
pymupdf
pyyaml
torch

# Packaging
pyinstaller>=6.0
